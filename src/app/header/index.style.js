import styled from 'styled-components';

export const StyledLogoContainer = styled.div`
	width: max-content;
	display: flex;
	align-items: flex-end;
	margin: 17px 0;

	& > p {
		margin-left: 10px;
	}
`;
